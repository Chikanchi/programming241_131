#include <iostream> // подключаем библиотеку для ввода/вывода
using namespace std; // упрощаем использование функций из библиотеки

int main() { // главная функция программы

    // 1. Объявляем две переменные для хранения чисел
    double num1, num2;

    // 2. Просим пользователя ввести два числа
    cout << "Введите первое число: "; 
    cin >> num1; // считываем первое число с клавиатуры

    cout << "Введите второе число: "; // выводим подсказку
    cin >> num2; 

    // 3. Вычисляем и выводим среднее арифметическое
    double average = (num1 + num2) / 2;
    cout << "Среднее арифметическое: " << average << endl; // выводим результат

    // 4. Объявляем переменную для знака операции
    char operation;

    // 5. Просим пользователя ввести знак операции
    cout << "Введите знак операции (+, -, *, /): "; 
    cin >> operation; // считываем знак операции с клавиатуры

    // 6. Используем условный оператор if-else для выполнения операции
    // (этот блок будем закомментировать позже)
    /*
    if (operation == '+') {
        cout << "Результат: " << num1 + num2 << endl;
    } else if (operation == '-') {
        cout << "Результат: " << num1 - num2 << endl;
    } else if (operation == '*') {
        cout << "Результат: " << num1 * num2 << endl;
    } else if (operation == '/') {
        if (num2 != 0) { // проверяем, что второе число не 0 (иначе ошибка деления)
            cout << "Результат: " << num1 / num2 << endl;
        } else {
            cout << "Ошибка: деление на ноль!" << endl;
        }
    } else {
        cout << "Неверный знак операции!" << endl;
    }
    */

    // 7. Реализуем тот же функционал с помощью switch-case
    switch (operation) { // switch проверяет значение переменной operation
        case '+':
            cout << "Результат: " << num1 + num2 << endl;
            break; 
        case '-': // 
            cout << "Результат: " << num1 - num2 << endl;
            break;

        case '*': // если operation равно '*'
            cout << "Результат: " << num1 * num2 << endl;
            break;

        case '/': // если operation равно '/'
            if (num2 != 0) {
                cout << "Результат: " << num1 / num2 << endl;
            } else {
                cout << "Ошибка: деление на ноль!" << endl;
            }
            break;

        default: // если ни один из случаев не подошёл (неверный знак)
            cout << "Неверный знак операции!" << endl;
            break;
    }


