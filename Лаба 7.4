#include <iostream>
#include <vector>
#include <map>
#include <string>

using namespace std;

class Settings {
public:
    static map<string, string> data;

    static void Add(string key, string value) {
        data[key] = value;
    }

    static string Get(string key) {
        return data[key];
    }

    static void Print() {
        for (auto i = data.begin(); i != data.end(); i++)
            cout << i->first << " = " << i->second << endl;
    }
};

void printVector(vector<float> v) {
    for (float n : v)
        cout << n << endl;
}

map<string, string> Settings::data;

int main() {
    vector<float> v;

    v.push_back(1.1);
    v.push_back(2.2);
    v.push_back(3.3);
    v.push_back(4.4);
    v.push_back(5.5);

    printVector(v);

    auto it = v.begin();
    it += 3;
    v.insert(it, 9.9);

    cout << "После вставки" << endl;
    printVector(v);

    v.pop_back();
    cout << "После удаления" << endl;
    printVector(v);

    Settings s;
    /*
    s.Add("Volume", "80");
    s.Add("Brightness", "50");
    s.Add("Resolution", "1920x1080");

    cout << s.Get("Volume") << endl;
    cout << s.Get("Brightness") << endl;
    cout << s.Get("Resolution") << endl;

    s.Print();
    */

    Settings::Add("Volume", "80");
    Settings::Add("Brightness", "50");
    Settings::Add("Resolution", "1920x1080");

    cout << Settings::Get("Volume") << endl;
    cout << Settings::Get("Brightness") << endl;
    cout << Settings::Get("Resolution") << endl;

    Settings::Print();
}
